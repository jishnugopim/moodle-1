/**
 * Provides generic keyboard navigation to aria-capable activities.
 *
 * @module moodle-core-keynav
 */

/**
 * Provides generic keyboard navigation to aria-capable activities.
 *
 * @class Moodle.core.KeyboardNavigation
 * @extends Plugin.Base
 */

Y.namespace('Moodle.core').KeyboardNavigation = Y.Base.create('keynav', Y.Plugin.Base, [], {
    initializer: function() {
        
    },

    destructor: function() {
    }
});
